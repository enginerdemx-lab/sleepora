<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ (API 31+) — Light mod. Sistem splash ekranında Sleepora
         ikonu + koyu arka plan. windowSplashScreenAnimatedIcon olarak yuvarlak
         köşeli + şeffaf dolgulu splash_icon kullanılır; düz kare ic_launcher
         kullanılınca sistem sert kare gösteriyordu. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
